import { Request, Response } from "express";

class CreateUserController {
  handle(request: Request, reponse: Response): Promise<Response> {
    // return response.status(200).send();
  }
}

export { CreateUserController };
